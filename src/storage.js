const KEY='poem-scholar-v1';
export const defaults={profile:null,settings:{sound:true,music:false,font:'normal',pinyin:true,minutes:10,taskCount:4,reduceMotion:false},progress:{},activities:[],daily:null,rewards:{points:0,gamesCompleted:0,poemsCompleted:0,minutesLearned:0},activeGame:null,parentUnlocked:false};
export const storage={load(){try{const saved=JSON.parse(localStorage.getItem(KEY)||'{}');return {...structuredClone(defaults),...saved,settings:{...defaults.settings,...saved.settings},rewards:{...defaults.rewards,...saved.rewards}}}catch{return structuredClone(defaults)}},save(state){localStorage.setItem(KEY,JSON.stringify(state))},clear(){localStorage.removeItem(KEY)}};
